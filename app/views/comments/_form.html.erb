<%= form_with(model: [commentable, comment], local: true) do |f| %>
  <div class="field">
    <%= f.label :comment %>
    <%= f.text_area :content, required: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
